// function to show numbers in the array if the array size is less than 20
export default function showNum(array,index){
    if (array.length <20){
      return array[index];
    }
    else{
      return;
    }
  }